# Shelly CLI Documentation

> Auto-generated by `shelly docs`.

## Overview

Shelly — a modern, unified package manager for Arch Linux. Install, update, search, and manage standard (ALPM) packages, the AUR, Flatpaks, and AppImages from a single command-line interface.

### Direct Commands (shelly)

| Command                      | Description                                                           |
|------------------------------|-----------------------------------------------------------------------|
| `query [package]`            | Query repositories and packages                                       |
| `install [packages...]`      | Install packages                                                      |
| `upgrade`                    | Perform a full system upgrade                                         |
| `upgrade-all`                | Upgrade all packages from every source (repo, AUR, Flatpak, AppImage) |
| `downgrade [package]`        | Downgrade a package                                                   |
| `ignore [packages...]`       | Manage ignored packages                                               |
| `news`                       | Show ArchLinux news                                                   |
| `cache-clean`                | Clean the local cache                                                 |
| `check-updates`              | Check for package updates                                             |
| `list-updates`               | List standard packages that have available updates                    |
| `export`                     | Export the current system state to a file                             |
| `fix-permissions`            | Fix permissions for Shelly directories                                |
| `mark [package]`             | Mark a package as explicit or a dependency                            |
| `purify`                     | Remove corrupted/orphaned packages                                    |
| `remove [packages...]`       | Remove packages (repo or local binary)                                |
| `sync`                       | Syncs the system with the current state                               |
| `update [packages...]`       | Update specific packages                                              |
| `docs`                       | Generate Markdown reference documentation for the Shelly CLI          |
| `keyring <action> [keys...]` | Manage the pacman keyring                                             |

### Manage AppImages (`shelly appimage`)

| Command                                              | Description                                    |
|------------------------------------------------------|------------------------------------------------|
| `appimage install <location>`                        | Install an AppImage                            |
| `appimage remove <appimage>`                         | Remove an AppImage                             |
| `appimage list`                                      | Lists all AppImages                            |
| `appimage upgrade`                                   | Upgrades all AppImages                         |
| `appimage sync-meta [package]`                       | Syncs meta data for an AppImage                |
| `appimage list-updates`                              | Find Updates for an AppImage                   |
| `appimage configure-updates <appimage> <url> <type>` | Configures the update settings for an AppImage |
| `appimage migrate-manager`                           | Updates the AppImage manager version           |

### Manage shelly configuration (`shelly config`)

| Command                           | Description                          |
|-----------------------------------|--------------------------------------|
| `config get <key>`                | Get a configuration value            |
| `config set <key> <value>`        | Set a configuration value            |
| `config list`                     | List all configuration values        |
| `config reset`                    | Reset the configuration to defaults  |
| `config parallel <downloadCount>` | Set the number of parallel downloads |

### Manage AUR packages (`shelly aur`)

| Command                                  | Description                                    |
|------------------------------------------|------------------------------------------------|
| `aur install [packages...]`              | Install AUR packages                           |
| `aur install-version <package> <commit>` | Install a specific version of an AUR package   |
| `aur remove [packages...]`               | Remove AUR packages                            |
| `aur update [packages...]`               | Rebuild and reinstall specific AUR packages    |
| `aur upgrade`                            | Upgrade all out-of-date AUR packages           |
| `aur list`                               | List installed AUR packages                    |
| `aur list-updates`                       | List AUR packages that have available updates  |
| `aur search <query...>`                  | Search the Arch User Repository                |
| `aur search-pkgbuild <packages...>`      | Fetch and display the PKGBUILD of AUR packages |

### Manage flatpak (`shelly flatpak`)

| Command                                          | Description                           |
|--------------------------------------------------|---------------------------------------|
| `flatpak install <package>`                      | Install flatpak app                   |
| `flatpak update <package>`                       | Update flatpak app                    |
| `flatpak list`                                   | List installed flatpak apps           |
| `flatpak list-updates`                           | List flatpak apps with updates        |
| `flatpak running`                                | List running flatpak apps             |
| `flatpak repair`                                 | Repairs Flatpak Installation          |
| `flatpak uninstall <package>`                    | Remove flatpak app                    |
| `flatpak run <package>`                          | Run flatpak app                       |
| `flatpak kill <package>`                         | Kill running flatpak app              |
| `flatpak search <query>`                         | Search flatpak                        |
| `flatpak sync-remote-appstream`                  | Sync remote appstream                 |
| `flatpak get-remote-appstream <query>`           | Returns remote appstream json         |
| `flatpak upgrade`                                | Upgrade all flatpak apps              |
| `flatpak list-remotes`                           | Returns all remotes currently added   |
| `flatpak add-remotes <remote>`                   | Adds a flatpak remote                 |
| `flatpak remove-remotes <remote>`                | Removes a flatpak remote              |
| `flatpak install-ref-file <RefFilePath>`         | Installs flatpak app from ref file    |
| `flatpak install-bundle <BundlePath>`            | Installs flatpak app from bundle file |
| `flatpak app-remote-info <remote> <id> <branch>` | Get app remote info                   |


---

### Shortcodes

Grammar: `-<Type><Action><modifiers...>` [positionals]

Type selects the command group, Action selects the verb, and modifiers are that verb's own short flags (case-sensitive).

| Type | Group |
|------|-------|
| `I`  | appimage |
| `A`  | aur |
| `C`  | config |
| `F`  | flatpak |
| `K`  | keyring |
| `S`  | standard |
| `U`  | utility |

Examples:

```sh
-SIu firefox   ->  install -u firefox
-AS query      ->  aur search query
-KV ABCD       ->  keyring recv ABCD
```

> In shortcode mode use `--ui-mode` instead of `-U`.

#### Actions by Type

**`I` — appimage**

| Action | Verb | Modifiers |
|--------|------|-----------|
| `I` | install | — |
| `R` | remove | `c` |
| `L` | list | — |
| `U` | upgrade | — |
| `P` | list-updates | — |
| `S` | sync-meta | — |
| `C` | configure-updates | `p` |
| `M` | migrate-manager | — |

**`A` — aur**

| Action | Verb | Modifiers |
|--------|------|-----------|
| `I` | install | `c`, `m`, `o` |
| `V` | install-version | — |
| `R` | remove | `c`, `i`, `o` |
| `T` | update | — |
| `U` | upgrade | — |
| `L` | list | — |
| `P` | list-updates | — |
| `S` | search | — |
| `B` | search-pkgbuild | — |

**`C` — config**

| Action | Verb | Modifiers |
|--------|------|-----------|
| `G` | get | — |
| `S` | set | — |
| `L` | list | — |
| `R` | reset | — |
| `P` | parallel | — |

**`F` — flatpak**

| Action | Verb | Modifiers |
|--------|------|-----------|
| `I` | install | `b`, `r` |
| `T` | update | — |
| `U` | upgrade | — |
| `L` | list | — |
| `P` | list-updates | — |
| `R` | uninstall | `c`, `r` |
| `N` | running | — |
| `X` | run | — |
| `K` | kill | — |
| `S` | search | `l`, `p` |
| `H` | repair | — |
| `M` | list-remotes | — |
| `A` | add-remotes | `g`, `s`, `u` |
| `D` | remove-remotes | `s` |
| `E` | install-ref-file | `s` |
| `B` | install-bundle | `s` |
| `Y` | sync-remote-appstream | — |
| `G` | get-remote-appstream | — |
| `O` | app-remote-info | — |

**`K` — keyring**

| Action | Verb |
|--------|------|
| `I` | init |
| `L` | list |
| `R` | refresh |
| `S` | lsign |
| `P` | populate |
| `V` | recv |

**`S` — standard**

| Action | Verb | Modifiers |
|--------|------|-----------|
| `I` | install | `b`, `d`, `m`, `u` |
| `R` | remove | `c`, `i`, `l`, `o`, `r` |
| `Q` | query | `a`, `d`, `i`, `l`, `p`, `r`, `t`, `w` |
| `M` | mark | `d`, `e` |
| `Y` | sync | `f` |
| `P` | purify | `d`, `o` |
| `N` | news | `a` |
| `D` | downgrade | `i`, `l`, `o`, `t` |
| `G` | ignore | `a`, `c`, `l`, `r` |
| `U` | upgrade | — |
| `T` | update | — |

**`U` — utility**

| Action | Verb | Modifiers |
|--------|------|-----------|
| `C` | cache-clean | `c`, `d`, `i`, `k`, `t` |
| `K` | check-updates | `a`, `c`, `l` |
| `E` | export | `a`, `o` |
| `F` | fix-permissions | — |

---


## `shelly`

Shelly — a modern, unified package manager for Arch Linux. Install, update, search, and manage standard (ALPM) packages, the AUR, Flatpaks, and AppImages from a single command-line interface.

*Branch command — use one of the subcommands below.*

**Subcommands:**

- [`shelly query`](#shelly-query) — Query repositories and packages
- [`shelly install`](#shelly-install) — Install packages
- [`shelly upgrade`](#shelly-upgrade) — Perform a full system upgrade
- [`shelly upgrade-all`](#shelly-upgrade-all) — Upgrade all packages from every source (repo, AUR, Flatpak, AppImage)
- [`shelly downgrade`](#shelly-downgrade) — Downgrade a package
- [`shelly ignore`](#shelly-ignore) — Manage ignored packages
- [`shelly news`](#shelly-news) — Show ArchLinux news
- [`shelly cache-clean`](#shelly-cache-clean) — Clean the local cache
- [`shelly check-updates`](#shelly-check-updates) — Check for package updates
- [`shelly list-updates`](#shelly-list-updates) — List standard packages that have available updates
- [`shelly export`](#shelly-export) — Export the current system state to a file
- [`shelly fix-permissions`](#shelly-fix-permissions) — Fix permissions for Shelly directories
- [`shelly mark`](#shelly-mark) — Mark a package as explicit or a dependency
- [`shelly purify`](#shelly-purify) — Remove corrupted/orphaned packages
- [`shelly remove`](#shelly-remove) — Remove packages (repo or local binary)
- [`shelly sync`](#shelly-sync) — Syncs the system with the current state
- [`shelly update`](#shelly-update) — Update specific packages
- [`shelly docs`](#shelly-docs) — Generate Markdown reference documentation for the Shelly CLI
- [`shelly appimage`](#shelly-appimage) — Manage AppImages
- [`shelly config`](#shelly-config) — Manage shelly configuration
- [`shelly aur`](#shelly-aur) — Manage AUR packages
- [`shelly keyring`](#shelly-keyring) — Manage the pacman keyring
- [`shelly flatpak`](#shelly-flatpak) — Manage flatpak

**Usage:**

```text
shelly
```


### `shelly query`

Query repositories and packages

**Usage:**

```text
shelly query [package]
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<package>` | 0..1 | The package to search for |


### `shelly install`

Install packages

**Usage:**

```text
shelly install [packages...]
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<packages>` | 0..100000 | The packages to install (repo names, local files or URLs) |


### `shelly upgrade`

Perform a full system upgrade

**Usage:**

```text
shelly upgrade
```


### `shelly upgrade-all`

Upgrade all packages from every source (repo, AUR, Flatpak, AppImage)

**Usage:**

```text
shelly upgrade-all
```


### `shelly downgrade`

Downgrade a package

**Usage:**

```text
shelly downgrade [package]
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<package>` | 0..1 | The package to downgrade |


### `shelly ignore`

Manage ignored packages

**Usage:**

```text
shelly ignore [packages...]
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<packages>` | 0..100000 | The packages to interact with |


### `shelly news`

Show ArchLinux news

**Usage:**

```text
shelly news
```


### `shelly cache-clean`

Clean the local cache

**Usage:**

```text
shelly cache-clean
```


### `shelly check-updates`

Check for package updates

**Usage:**

```text
shelly check-updates
```


### `shelly list-updates`

List standard packages that have available updates

**Usage:**

```text
shelly list-updates
```


### `shelly export`

Export the current system state to a file

**Usage:**

```text
shelly export
```


### `shelly fix-permissions`

Fix permissions for Shelly directories

**Usage:**

```text
shelly fix-permissions
```


### `shelly mark`

Mark a package as explicit or a dependency

**Usage:**

```text
shelly mark [package]
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<package>` | 0..1 | The package to mark |


### `shelly purify`

Remove corrupted/orphaned packages

**Usage:**

```text
shelly purify
```


### `shelly remove`

Remove packages (repo or local binary)

**Usage:**

```text
shelly remove [packages...]
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<packages>` | 0..100000 | The packages to remove (repo names or local binary packages) |


### `shelly sync`

Syncs the system with the current state

**Usage:**

```text
shelly sync
```


### `shelly update`

Update specific packages

**Usage:**

```text
shelly update [packages...]
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<packages>` | 0..100000 | The packages to update |


### `shelly docs`

Generate Markdown reference documentation for the Shelly CLI

**Usage:**

```text
shelly docs
```


### `shelly appimage`

Manage AppImages

*Branch command — use one of the subcommands below.*

**Subcommands:**

- [`shelly appimage install`](#shelly-appimage-install) — Install an AppImage
- [`shelly appimage remove`](#shelly-appimage-remove) — Remove an AppImage
- [`shelly appimage list`](#shelly-appimage-list) — Lists all AppImages
- [`shelly appimage upgrade`](#shelly-appimage-upgrade) — Upgrades all AppImages
- [`shelly appimage sync-meta`](#shelly-appimage-sync-meta) — Syncs meta data for an AppImage
- [`shelly appimage list-updates`](#shelly-appimage-list-updates) — Find Updates for an AppImage
- [`shelly appimage configure-updates`](#shelly-appimage-configure-updates) — Configures the update settings for an AppImage
- [`shelly appimage migrate-manager`](#shelly-appimage-migrate-manager) — Updates the AppImage manager version

**Usage:**

```text
shelly appimage
```


#### `shelly appimage install`

Install an AppImage

**Usage:**

```text
shelly appimage install <location>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<location>` | 1 | Location of the AppImage |


#### `shelly appimage remove`

Remove an AppImage

**Usage:**

```text
shelly appimage remove <appimage>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<appimage>` | 1 | Name of the AppImage |


#### `shelly appimage list`

Lists all AppImages

**Usage:**

```text
shelly appimage list
```


#### `shelly appimage upgrade`

Upgrades all AppImages

**Usage:**

```text
shelly appimage upgrade
```


#### `shelly appimage sync-meta`

Syncs meta data for an AppImage

**Usage:**

```text
shelly appimage sync-meta [package]
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<package>` | 0..1 | The search query for the AppImage |


#### `shelly appimage list-updates`

Find Updates for an AppImage

**Usage:**

```text
shelly appimage list-updates
```


#### `shelly appimage configure-updates`

Configures the update settings for an AppImage

**Usage:**

```text
shelly appimage configure-updates <appimage> <url> <type>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<appimage>` | 1 | AppImage name to configure updates |
| `<url>` | 1 | Update URL |
| `<type>` | 1 | Update Type |


#### `shelly appimage migrate-manager`

Updates the AppImage manager version

**Usage:**

```text
shelly appimage migrate-manager
```


### `shelly config`

Manage shelly configuration

*Branch command — use one of the subcommands below.*

**Subcommands:**

- [`shelly config get`](#shelly-config-get) — Get a configuration value
- [`shelly config set`](#shelly-config-set) — Set a configuration value
- [`shelly config list`](#shelly-config-list) — List all configuration values
- [`shelly config reset`](#shelly-config-reset) — Reset the configuration to defaults
- [`shelly config parallel`](#shelly-config-parallel) — Set the number of parallel downloads

**Usage:**

```text
shelly config
```


#### `shelly config get`

Get a configuration value

**Usage:**

```text
shelly config get <key>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<key>` | 1 | The configuration key to get |


#### `shelly config set`

Set a configuration value

**Usage:**

```text
shelly config set <key> <value>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<key>` | 1 | The configuration key to set |
| `<value>` | 1 | The value to set |


#### `shelly config list`

List all configuration values

**Usage:**

```text
shelly config list
```


#### `shelly config reset`

Reset the configuration to defaults

**Usage:**

```text
shelly config reset
```


#### `shelly config parallel`

Set the number of parallel downloads

**Usage:**

```text
shelly config parallel <downloadCount>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<downloadCount>` | 1 | The number of parallel downloads |


### `shelly aur`

Manage AUR packages

*Branch command — use one of the subcommands below.*

**Subcommands:**

- [`shelly aur install`](#shelly-aur-install) — Install AUR packages
- [`shelly aur install-version`](#shelly-aur-install-version) — Install a specific version of an AUR package
- [`shelly aur remove`](#shelly-aur-remove) — Remove AUR packages
- [`shelly aur update`](#shelly-aur-update) — Rebuild and reinstall specific AUR packages
- [`shelly aur upgrade`](#shelly-aur-upgrade) — Upgrade all out-of-date AUR packages
- [`shelly aur list`](#shelly-aur-list) — List installed AUR packages
- [`shelly aur list-updates`](#shelly-aur-list-updates) — List AUR packages that have available updates
- [`shelly aur search`](#shelly-aur-search) — Search the Arch User Repository
- [`shelly aur search-pkgbuild`](#shelly-aur-search-pkgbuild) — Fetch and display the PKGBUILD of AUR packages

**Usage:**

```text
shelly aur
```


#### `shelly aur install`

Install AUR packages

**Usage:**

```text
shelly aur install [packages...]
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<packages>` | 0..100000 | One or more AUR package names to operate on (space-separated) |


#### `shelly aur install-version`

Install a specific version of an AUR package

**Usage:**

```text
shelly aur install-version <package> <commit>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<package>` | 1 | Name of the AUR package to install |
| `<commit>` | 1 | Git commit hash specifying the exact version to install |


#### `shelly aur remove`

Remove AUR packages

**Usage:**

```text
shelly aur remove [packages...]
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<packages>` | 0..100000 | One or more AUR package names to operate on (space-separated) |


#### `shelly aur update`

Rebuild and reinstall specific AUR packages

**Usage:**

```text
shelly aur update [packages...]
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<packages>` | 0..100000 | One or more AUR package names to operate on (space-separated) |


#### `shelly aur upgrade`

Upgrade all out-of-date AUR packages

**Usage:**

```text
shelly aur upgrade
```


#### `shelly aur list`

List installed AUR packages

**Usage:**

```text
shelly aur list
```


#### `shelly aur list-updates`

List AUR packages that have available updates

**Usage:**

```text
shelly aur list-updates
```


#### `shelly aur search`

Search the Arch User Repository

**Usage:**

```text
shelly aur search <query...>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<query>` | 1..100000 | Search term to find packages in the Arch User Repository |


#### `shelly aur search-pkgbuild`

Fetch and display the PKGBUILD of AUR packages

**Usage:**

```text
shelly aur search-pkgbuild <packages...>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<packages>` | 1..100000 | One or more AUR package names to fetch the PKGBUILD for |


### `shelly keyring`

Manage the pacman keyring

**Usage:**

```text
shelly keyring <action> [keys...]
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<action>` | 1 | The keyring action to perform |
| `<keys>` | 0..100000 | The key IDs to operate on |


### `shelly flatpak`

Manage flatpak

*Branch command — use one of the subcommands below.*

**Subcommands:**

- [`shelly flatpak install`](#shelly-flatpak-install) — Install flatpak app
- [`shelly flatpak update`](#shelly-flatpak-update) — Update flatpak app
- [`shelly flatpak list`](#shelly-flatpak-list) — List installed flatpak apps
- [`shelly flatpak list-updates`](#shelly-flatpak-list-updates) — List flatpak apps with updates
- [`shelly flatpak running`](#shelly-flatpak-running) — List running flatpak apps
- [`shelly flatpak repair`](#shelly-flatpak-repair) — Repairs Flatpak Installation
- [`shelly flatpak uninstall`](#shelly-flatpak-uninstall) — Remove flatpak app
- [`shelly flatpak run`](#shelly-flatpak-run) — Run flatpak app
- [`shelly flatpak kill`](#shelly-flatpak-kill) — Kill running flatpak app
- [`shelly flatpak search`](#shelly-flatpak-search) — Search flatpak
- [`shelly flatpak sync-remote-appstream`](#shelly-flatpak-sync-remote-appstream) — Sync remote appstream
- [`shelly flatpak get-remote-appstream`](#shelly-flatpak-get-remote-appstream) — Returns remote appstream json
- [`shelly flatpak upgrade`](#shelly-flatpak-upgrade) — Upgrade all flatpak apps
- [`shelly flatpak list-remotes`](#shelly-flatpak-list-remotes) — Returns all remotes currently added
- [`shelly flatpak add-remotes`](#shelly-flatpak-add-remotes) — Adds a flatpak remote
- [`shelly flatpak remove-remotes`](#shelly-flatpak-remove-remotes) — Removes a flatpak remote
- [`shelly flatpak install-ref-file`](#shelly-flatpak-install-ref-file) — Installs flatpak app from ref file
- [`shelly flatpak install-bundle`](#shelly-flatpak-install-bundle) — Installs flatpak app from bundle file
- [`shelly flatpak app-remote-info`](#shelly-flatpak-app-remote-info) — Get app remote info

**Usage:**

```text
shelly flatpak
```


#### `shelly flatpak install`

Install flatpak app

**Usage:**

```text
shelly flatpak install <package>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<package>` | 1 | Flatpak application ID (e.g., com.spotify.Client) |


#### `shelly flatpak update`

Update flatpak app

**Usage:**

```text
shelly flatpak update <package>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<package>` | 1 | Flatpak application ID (e.g., com.spotify.Client) |


#### `shelly flatpak list`

List installed flatpak apps

**Usage:**

```text
shelly flatpak list
```


#### `shelly flatpak list-updates`

List flatpak apps with updates

**Usage:**

```text
shelly flatpak list-updates
```


#### `shelly flatpak running`

List running flatpak apps

**Usage:**

```text
shelly flatpak running
```


#### `shelly flatpak repair`

Repairs Flatpak Installation

**Usage:**

```text
shelly flatpak repair
```


#### `shelly flatpak uninstall`

Remove flatpak app

**Usage:**

```text
shelly flatpak uninstall <package>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<package>` | 1 | Flatpak application ID (e.g., com.spotify.Client) |


#### `shelly flatpak run`

Run flatpak app

**Usage:**

```text
shelly flatpak run <package>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<package>` | 1 | Flatpak application ID (e.g., com.spotify.Client) |


#### `shelly flatpak kill`

Kill running flatpak app

**Usage:**

```text
shelly flatpak kill <package>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<package>` | 1 | Flatpak application ID (e.g., com.spotify.Client) |


#### `shelly flatpak search`

Search flatpak

**Usage:**

```text
shelly flatpak search <query>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<query>` | 1 | Search term to find Flatpak applications on Flathub |


#### `shelly flatpak sync-remote-appstream`

Sync remote appstream

**Usage:**

```text
shelly flatpak sync-remote-appstream
```


#### `shelly flatpak get-remote-appstream`

Returns remote appstream json

**Usage:**

```text
shelly flatpak get-remote-appstream <query>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<query>` | 1 | Gets appstream data in json (use all to retrieve all appstreams) |


#### `shelly flatpak upgrade`

Upgrade all flatpak apps

**Usage:**

```text
shelly flatpak upgrade
```


#### `shelly flatpak list-remotes`

Returns all remotes currently added

**Usage:**

```text
shelly flatpak list-remotes
```


#### `shelly flatpak add-remotes`

Adds a flatpak remote

**Usage:**

```text
shelly flatpak add-remotes <remote>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<remote>` | 1 | Flatpak remote name ID (e.g., flathub) |


#### `shelly flatpak remove-remotes`

Removes a flatpak remote

**Usage:**

```text
shelly flatpak remove-remotes <remote>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<remote>` | 1 | Flatpak remote name ID (e.g., flathub) |


#### `shelly flatpak install-ref-file`

Installs flatpak app from ref file

**Usage:**

```text
shelly flatpak install-ref-file <RefFilePath>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<RefFilePath>` | 1 | Path to the ref file |


#### `shelly flatpak install-bundle`

Installs flatpak app from bundle file

**Usage:**

```text
shelly flatpak install-bundle <BundlePath>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<BundlePath>` | 1 | Path to the .flatpak bundle file |


#### `shelly flatpak app-remote-info`

Get app remote info

**Usage:**

```text
shelly flatpak app-remote-info <remote> <id> <branch>
```

| Argument | Arity | Description |
|----------|-------|-------------|
| `<remote>` | 1 | Flatpak remote name (e.g., flathub) |
| `<id>` | 1 | Flatpak application ID |
| `<branch>` | 1 | Branch to query (e.g., stable) |

